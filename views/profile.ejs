<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen  justify-center bg-zinc-900 text-white p-10">
    <div class="flex justify-end">
      <a href="/logout" class="px-3 py-2 bg-red-600 rounded-md ">Logout</a>
    </div>
      <h3 class="mb-5 text-3xl text-white "><span class="font-light">Hello</span>, <%= user.name%>❤️</h3>
      <h5 class="mb-4">You can create a new post.</h5>
      <form action="/post" method="post">
        <textarea placeholder="Whats on your mind?" class="block p-3 outline-none resize-none w-1/3 bg-transparent border-2 border-zinc-800 rounded-md " name="content"></textarea>
        <input type="submit" value="Create New Post" class="block bg-blue-500 px-4 py-3 rounded-md mt-2 text-sm ">
      </form>
      <div class="mt-20 text-zinc-400">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 mb-3 p-4 border-[1px] rounded-md border-zinc-700 bg-zinc-800">
              <h4 class="mb-2 text-white">@<%= user.username %></h4>
              <p class="tracking-tight"><%= post.content %></p>
              <small class="inline-block mt-2 font-bold"><%= post.likes.length %> likes </small>
              <div class="btn flex gap-5 mt-1">
                <a class="text-blue-400" href="like/<%= post._id%>">
                  <%= post.likes.indexOf(user._id)=== -1 ?"Like":"Unlike" %>
                </a>
                <a href="/edit/<%= post._id %>">Edit</a>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    </div>
</body>
</html>